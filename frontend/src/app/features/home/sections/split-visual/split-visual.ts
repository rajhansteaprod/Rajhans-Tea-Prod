import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-split-visual',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './split-visual.html',
  styleUrls: ['./split-visual.scss'],
})
export class SplitVisualComponent {}
