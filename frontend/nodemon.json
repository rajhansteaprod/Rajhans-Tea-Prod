{
  "watch": ["src", "tsconfig.server.json"],
  "ext": "ts,html,scss",
  "ignore": ["src/**/*.spec.ts", "dist/**/*"],
  "delay": 1000,
  "exec": "npm run build:ssr && npm run serve:ssr",
  "env": {
    "NODE_ENV": "development"
  }
}
